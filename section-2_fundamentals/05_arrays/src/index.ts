let numbersOne: number[] = [1, 2, 3];
let numbersTwo = [1, 2, 3];

let anyArray = []; // any array : avoid this!
let emptyNumArray: number[] = [];

numbersOne.forEach((n) => n.toFixed); // only offers method for numbers because n is a number!
