// tuples : array with fixed size where element has a particular type (doesn't have 2 be unique)
// try to restrict tuples to 2 values! like key value pairs


let user: [number, string, boolean] = [1, 'Arthur'];

user[0].toFixed; // only shows methods for numbers!
user[1].toLocaleLowerCase; // only shows methods for strings!

user.push(1);
